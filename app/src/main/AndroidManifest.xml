<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android">

    <uses-permission android:name="android.permission.READ_CONTACTS" />
    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.RECEIVE_BOOT_COMPLETED" />
    <uses-permission android:name="com.edilmilan.ge360.permission.INTEGRATION" />

    <queries>
        <package android:name="com.edilmilanstevic.attrezzi" />
        <package android:name="com.ge360.messaggi" />
    </queries>

    <application
        android:allowBackup="true"
        android:icon="@drawable/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@drawable/ic_launcher"
        android:supportsRtl="true"
        android:theme="@style/Theme.EdilClienti">

        <service
            android:name=".AutoBackupJobService"
            android:exported="false"
            android:permission="android.permission.BIND_JOB_SERVICE" />

        <receiver
            android:name=".Ge360LocalBridgeReceiver"
            android:exported="true"
            android:permission="com.edilmilan.ge360.permission.INTEGRATION">
            <intent-filter>
                <action android:name="com.edilmilan.ge360.local.PING" />
            </intent-filter>
        </receiver>

        <receiver
            android:name=".Ge360HandoffAckReceiver"
            android:exported="true"
            android:permission="com.edilmilan.ge360.permission.INTEGRATION">
            <intent-filter>
                <action android:name="com.edilmilan.ge360.action.JOBSITE_HANDOFF_ACK" />
            </intent-filter>
        </receiver>

        <activity
            android:name=".Ge360DeepLinkActivity"
            android:exported="true"
            android:noHistory="true">
            <intent-filter>
                <action android:name="android.intent.action.VIEW" />
                <category android:name="android.intent.category.DEFAULT" />
                <category android:name="android.intent.category.BROWSABLE" />
                <data android:scheme="ge360" android:host="clienti" />
            </intent-filter>
        </activity>

        <activity
            android:name=".ContactImportActivity"
            android:exported="false" />

        <activity
            android:name=".ClientDetailActivity"
            android:exported="false" />

        <activity
            android:name=".EditClientActivity"
            android:exported="false" />

        <activity
            android:name=".MainActivity"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>
