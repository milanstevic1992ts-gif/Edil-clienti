<?xml version="1.0" encoding="utf-8"?>
<LinearLayout xmlns:android="http://schemas.android.com/apk/res/android"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:orientation="vertical"
    android:background="@color/surface_alt">

    <LinearLayout
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:gravity="center_vertical"
        android:orientation="horizontal"
        android:padding="14dp"
        android:background="@color/black">

        <Button
            android:id="@+id/backButton"
            android:layout_width="48dp"
            android:layout_height="44dp"
            android:background="@drawable/button_dark"
            android:text="‹"
            android:textColor="@color/white"
            android:textSize="28sp" />

        <TextView
            android:id="@+id/screenTitle"
            android:layout_width="0dp"
            android:layout_height="wrap_content"
            android:layout_marginStart="10dp"
            android:layout_weight="1"
            android:text="Nuovo cliente"
            android:textColor="@color/white"
            android:textSize="22sp"
            android:textStyle="bold" />
    </LinearLayout>

    <ScrollView
        android:layout_width="match_parent"
        android:layout_height="0dp"
        android:layout_weight="1"
        android:fillViewport="true">

        <LinearLayout
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:orientation="vertical"
            android:padding="16dp">

            <EditText android:id="@+id/firstNameInput" style="@style/ClientField" android:hint="Nome *" android:inputType="textCapWords" />
            <EditText android:id="@+id/lastNameInput" style="@style/ClientField" android:hint="Cognome" android:inputType="textCapWords" />
            <EditText android:id="@+id/phoneInput" style="@style/ClientField" android:hint="Telefono" android:inputType="phone" />
            <EditText android:id="@+id/emailInput" style="@style/ClientField" android:hint="Email" android:inputType="textEmailAddress" />
            <EditText android:id="@+id/addressInput" style="@style/ClientField" android:hint="Indirizzo / luogo del lavoro" android:inputType="textPostalAddress|textCapSentences" />
            <EditText android:id="@+id/notesInput" style="@style/ClientField" android:hint="Note sul cliente o lavoro richiesto" android:gravity="top" android:inputType="textMultiLine|textCapSentences" android:minLines="4" />

            <CheckBox
                android:id="@+id/saveToContactsCheck"
                android:layout_width="match_parent"
                android:layout_height="wrap_content"
                android:layout_marginTop="4dp"
                android:buttonTint="@color/orange"
                android:padding="8dp"
                android:text="Salva anche nella rubrica Google / telefono"
                android:textColor="@color/black"
                android:textStyle="bold" />

            <TextView
                android:layout_width="match_parent"
                android:layout_height="wrap_content"
                android:paddingStart="8dp"
                android:paddingEnd="8dp"
                android:paddingBottom="10dp"
                android:text="Android aprirà la schermata contatto già compilata: scegli l’account Google o Telefono e conferma."
                android:textColor="@color/text_secondary"
                android:textSize="12sp" />

            <Button
                android:id="@+id/saveButton"
                android:layout_width="match_parent"
                android:layout_height="54dp"
                android:background="@drawable/button_primary"
                android:text="Salva cliente"
                android:textAllCaps="false"
                android:textColor="@color/black"
                android:textSize="17sp"
                android:textStyle="bold" />

            <Button
                android:id="@+id/deleteButton"
                android:layout_width="match_parent"
                android:layout_height="48dp"
                android:layout_marginTop="10dp"
                android:background="@drawable/button_dark"
                android:text="Elimina cliente"
                android:textAllCaps="false"
                android:textColor="@color/white"
                android:visibility="gone" />
        </LinearLayout>
    </ScrollView>
</LinearLayout>
